import styled from 'styled-components';
import {
  flexbox,
  layout,
  space,
} from 'styled-system';

const Form = styled.form`
  font-family: 'Montserrat', sans-serif;
  ${layout}
  ${flexbox}
  ${space}
`;

export default Form;
